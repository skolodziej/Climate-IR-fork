{
  "title": "Climate IR",
  "config": {
    "step": {
      "user": {
        "title": "Select the indoor unit family",
        "description": "Different Mitsubishi Heavy Industries remotes use completely different IR protocols. Pick the family that matches your indoor unit and remote.",
        "data": {
          "protocol": "Indoor unit family"
        }
      },
      "settings": {
        "title": "Add MHI air conditioner",
        "description": "Create a climate entity for {model}, linked to a native Home Assistant infrared emitter.",
        "data": {
          "name": "Name",
          "emitter_entity_id": "Infrared emitter",
          "base_frame_hex": "Base IR frame",
          "temperature_sensor": "Room temperature sensor",
          "humidity_sensor": "Room humidity sensor"
        },
        "data_description": {
          "base_frame_hex": "A 19-byte MHI base frame as hexadecimal. The default is the frame used by the tested ZSA-series remotes.",
          "temperature_sensor": "Optional sensor shown as the climate entity current temperature.",
          "humidity_sensor": "Optional sensor shown as the climate entity current humidity."
        }
      }
    },
    "error": {
      "invalid_base_frame": "The base frame must be valid hexadecimal that decodes to exactly 19 bytes.",
      "invalid_emitter": "Select an available infrared emitter entity.",
      "invalid_sensor": "Select an available sensor entity or clear this field."
    },
    "abort": {
      "no_emitters": "No native infrared emitter entities were found. Install and configure an infrared emitter integration first."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Climate IR options",
        "description": "Update the linked infrared emitter, base frame, or room sensors for {model}. To change the indoor unit family, add a new entry.",
        "data": {
          "name": "Name",
          "emitter_entity_id": "Infrared emitter",
          "base_frame_hex": "Base IR frame",
          "temperature_sensor": "Room temperature sensor",
          "humidity_sensor": "Room humidity sensor"
        },
        "data_description": {
          "base_frame_hex": "A 19-byte MHI base frame as hexadecimal. The default is the frame used by the tested ZSA-series remotes.",
          "temperature_sensor": "Optional sensor shown as the climate entity current temperature.",
          "humidity_sensor": "Optional sensor shown as the climate entity current humidity."
        }
      }
    },
    "error": {
      "invalid_base_frame": "The base frame must be valid hexadecimal that decodes to exactly 19 bytes.",
      "invalid_emitter": "Select an available infrared emitter entity.",
      "invalid_sensor": "Select an available sensor entity or clear this field."
    }
  },
  "issues": {
    "invalid_configured_entities": {
      "title": "Invalid entities in {config_entry_title}",
      "description": "The Climate IR configuration **{config_entry_title}** references entities that are no longer available: {invalid_entities}. Open **Settings > Devices & services > Climate IR**, select **Configure**, and choose valid replacements or clear optional sensors."
    }
  }
}
